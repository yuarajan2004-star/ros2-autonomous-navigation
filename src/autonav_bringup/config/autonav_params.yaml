slam_toolbox:
  ros__parameters:
    use_sim_time: true
    mode: mapping

    odom_frame: odom
    map_frame: map
    base_frame: base_link

    resolution: 0.05
    max_laser_range: 20.0
    map_update_interval: 2.0

robot_state_publisher:
  ros__parameters:
    use_sim_time: true

rviz:
  ros__parameters:
    use_sim_time: true

bridge:
  ros__parameters:
    use_sim_time: true
